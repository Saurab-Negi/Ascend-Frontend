export type KeySoundData = {
  stKeyName: string;
  sound: string[]; 
  updatedAt: string;
  freq: number;
  category: string;
  stKeyId: string;
  createdAt: string; 
  id: string;
  name: string;
  color: string; 
  desc: string;  
};
